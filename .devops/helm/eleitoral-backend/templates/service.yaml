apiVersion: v1
kind: Service
metadata:
  name: {{ include "eleitoral-backend.fullname" . }}
  labels:
    {{- include "eleitoral-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eleitoral-backend.selectorLabels" . | nindent 4 }}
