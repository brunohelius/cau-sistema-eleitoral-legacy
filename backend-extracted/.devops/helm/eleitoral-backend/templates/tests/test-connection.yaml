apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eleitoral-backend.fullname" . }}-test-connection"
  labels:
    {{- include "eleitoral-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eleitoral-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
