apiVersion: v1
kind: Service
metadata:
  name: {{ include "eleitoral-frontend.fullname" . }}
  labels:
    {{- include "eleitoral-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eleitoral-frontend.selectorLabels" . | nindent 4 }}
